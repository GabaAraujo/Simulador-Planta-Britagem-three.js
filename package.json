{
  "name": "crushing-screening-3d-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MVP 3D + SCADA para planta de britagem e peneiramento em circuito fechado (Vite + TS + Three.js).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
